# Minimal requirements for CI/CD test runs
# Only packages required to import and run the core tests
# Compatible with Python 3.9, 3.10, 3.11

# Web Framework & API
Flask==3.1.0
Flask-SocketIO==5.3.6
flask-cors==5.0.0
Flask-JWT-Extended==4.6.0
Flask-Limiter==3.8.0
Werkzeug==3.1.3

# Security
cryptography>=3.4.0
PyJWT==2.10.1
python-dotenv==1.0.1

# Core AI (lightweight - no torch/tensorflow)
scikit-learn>=1.0.0
numpy<2.0.0

# Pydantic v1 - codebase uses v1 API (BaseModel, validator, etc.)
pydantic>=1.10.0,<2.0.0

# Async & Concurrency
aiofiles>=22.1.0
anyio>=3.6.0

# HTTP & Networking
httpx>=0.23.0
requests>=2.28.0
urllib3>=1.26.0

# System & Performance
psutil>=5.9.0
cachetools>=5.0.0

# Utilities
colorama>=0.4.6
packaging>=21.0
tqdm>=4.64.0

# Type Checking
typing_extensions>=4.0.0

# Data
networkx>=2.8

# Encoding
charset-normalizer>=2.0.0
